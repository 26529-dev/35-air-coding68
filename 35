name = input ("ชื่อของคุณ")
print ("สวัสดี, " + name + "!")
user_input = input("กรุณาใส่เวลา (เช่น 10:30): ")
user_input = input("ใส่เวลาที่จะไปถึง (เช่น 13:50):" )
s = float(input("ป้อนค่า s: "))
v = float(input("ป้อนค่า v: "))
t = (s/v)
print("ต้องใช้เวลา " + str(t) + "นาที")
